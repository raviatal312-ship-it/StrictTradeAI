# Codemagic build

The active Codemagic configuration is now at the repository root: `../codemagic.yaml`.

It builds from the `android/` directory and downloads Gradle 8.11.1 during the build.

Artifact: `android/app/build/outputs/apk/debug/app-debug.apk`
